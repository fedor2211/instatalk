<p id="notice"><%= notice %></p>

<div>
  <h1>Users online</h1>
  <div id="users-online" class="col-10">
  </div>
</div>

<h1>Rooms</h1>

<ul class="mt-4 list-unstyled">
  <%= render @rooms %>
</ul>

<div class="mt-2">
  <%= render 'rooms/form', room: @room %>
</div>
